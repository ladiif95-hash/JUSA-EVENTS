services:
  mongodb:
    image: mongo:7
    container_name: jusa-mongodb
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - jusa_mongodb_data:/data/db

volumes:
  jusa_mongodb_data:
