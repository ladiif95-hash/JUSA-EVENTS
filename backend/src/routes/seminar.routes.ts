import { Router } from 'express'; import * as controller from '../controllers/seminar.controller'; import * as registration from '../controllers/registration.controller'; import { requireAuth } from '../middleware/auth.middleware'; import { asyncHandler } from '../middleware/error.middleware';
export const seminarRouter = Router(); seminarRouter.get('/', asyncHandler(controller.listSeminars)); seminarRouter.get('/:slug', asyncHandler(controller.seminarDetails)); seminarRouter.post('/:id/register', requireAuth, asyncHandler(registration.register)); seminarRouter.post('/:id/cancel', requireAuth, asyncHandler(registration.cancel)); seminarRouter.post('/:id/waitlist', requireAuth, asyncHandler(registration.register));
