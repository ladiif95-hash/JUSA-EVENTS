import { useState } from 'react'; import { Link } from 'react-router-dom'; import { authService } from '../services/auth.service';
export default function ForgotPassword() { const [email, setEmail] = useState(''); const [sent, setSent] = useState(false); return <section className="page profile-page container"><span className="eyebrow">ACCOUNT RECOVERY</span><h1 className="page-title">Reset password</h1><p className="lead">Enter your email and we will send a secure reset link.</p><form className="profile-form" onSubmit={async event => { event.preventDefault(); await fetch(`${import.meta.env.VITE_API_URL || 'http://localhost:5000/api'}/auth/forgot-password`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email }) }); setSent(true); }}><label>Email address<input type="email" required value={email} onChange={e => setEmail(e.target.value)} placeholder="you@example.com"/></label>{sent && <p className="success-message">If the account exists, a reset link has been sent.</p>}<button className="button">Send reset link</button></form><p><Link className="text-link" to="/login">Back to sign in</Link></p></section>; }
